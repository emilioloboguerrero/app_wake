# Phase 0a — DOM-call sweep log (Task 5)

Temporary working log. Consumed by Task 8 (ARCHITECTURE.md), then deleted per Task 8's brief.

Generated by:
```
cd apps/pwa && grep -rn "window\.\|document\." src --include="*.js" | grep -v "\.web\.js" | grep -v "__tests__" | grep -v "typeof window" | grep -v "isWeb" | grep -v "Platform.OS === 'web'" > docs-notes-phase0a.md
```
90 raw hits across 32 files. Every hit triaged below.

Native nav tree used for reachability (per task brief): `AppNavigator` → `AuthNavigator` (LoginScreen) / `OnboardingNavigator` (OnboardingScreen + 7 questions + Complete) / `MainTabNavigator` → `MainStackNavigator` (MainScreen, ProgramLibrary, CourseDetail, CreatorProfile, DailyWorkout, Warmup, WorkoutExecution, WorkoutCompletion, CourseStructure, UpcomingCallDetail) + `ProfileStackNavigator` (ProfileHome, AllPurchasedCourses, Subscriptions, CourseDetail, ExercisePanel/PRsScreen, ExerciseDetail/PRDetailScreen, WeeklyVolumeHistory, Sessions, SessionDetail) — plus every service/util those screens import (transitively).

Result: **0 VIOLATIONS**. Every hit is either GUARDED (existing `isWeb`/`typeof window !== 'undefined'` early-return or conditional already wraps it — the grep's own filters just don't catch guards that live on an earlier/enclosing line rather than the same line) or UNREACHABLE-FROM-NATIVE-NAV (only ever imported by `App.web.js` or by a web-only screen/navigator, e.g. `LabScreen.js`, `NutritionScreen.js` via `WebAppNavigator.jsx`). A handful of lines are false-positive text matches (comments containing the word "document", not API calls).

Annotation counts: **GUARDED: 22 files (68 line-hits)** · **UNREACHABLE-FROM-NATIVE-NAV: 8 files (17 line-hits)** · **FALSE-POSITIVE (comment): 4 files (5 line-hits)** · **VIOLATION: 0**.

---

## GUARDED (existing idiom already wraps the call — no fix needed)

- `src/config/environment.js:69,70` — `isProductionDebug()`; guarded by `if (typeof window === 'undefined') return false;` at :67.
- `src/utils/imageColorUtils.js:13,18` — `getAverageColorFromImageUrl()`; guarded by `if (typeof window === 'undefined' || !window.document || !imageUrl) return Promise.resolve(null);` at :8. Used by `ProfileScreen.js` (reachable, ProfileHome).
- `src/utils/accentExtractor.js:15,20` — `extractAccentColor()`; guarded by the same `typeof window === 'undefined' || !window.document` early-return at :10. Used via `hooks/hoy/useAccentFromImage.js`, consumed by `WorkoutExecutionScreen.js` and `WorkoutCompletionScreen.js` (both reachable) — resolves `null` harmlessly on native. `applyAccentToElement()` (uses `el.style`, not window/document directly) is never called anywhere in the codebase — dead export, not a reachability concern.
- `src/utils/errorReporter.js:16,157,160` — `resolveIngestUrl()` guarded at :15, `installFlushOnHide()` guarded at :154, `reportError()` guarded at :115 — all `typeof window === 'undefined'` early-returns. Imported by `ErrorBoundary.js` (wraps every screen via `withErrorBoundary`).
- `src/utils/lastLoginMethod.js:22,23,39,40` — inside `if (isWeb) { if (typeof window !== 'undefined' && window.localStorage) {...} }` (double-guarded) at :20-21 / :36-37. Used by `LoginScreen.js` (reachable, AuthNavigator). Grep's `isWeb` filter didn't catch these because the guard is on an earlier line, not the hit line itself.
- `src/screens/ProfileScreen.js:605` — inside `if (typeof window !== 'undefined') {` at :603 (sign-out redirect). Reachable (ProfileHome).
- `src/utils/platform.js:19,33,58,69,70,83,84` — every use gated by the module-level `isWeb` const (`typeof window !== 'undefined' && typeof document !== 'undefined'`, :4) via `if (!isWeb) return ...` / `if (isWeb) {...}` in each exported function. This is the canonical idiom referenced by the brief. Imported (directly or transitively) by ~20 reachable files (AuthContext, ProfileScreen, OnboardingScreen, MainScreen, CourseDetailScreen, WorkoutExecutionScreen, WarmupScreen, profilePictureService, analyticsService, videoCacheService, authService, etc).
- `src/screens/OnboardingScreen.js:1410` — inside `if (typeof window !== 'undefined') {` at :1408 (cancel-onboarding sign-out redirect). Reachable (OnboardingProfile / OnboardingNavigator entry).
- `src/screens/PRDetailScreen.js:79` — inside `if (typeof window !== 'undefined' && window.history) {` at :78 (back-navigation fallback). Reachable (ProfileStackNavigator → ExerciseDetail).
- `src/screens/WarmupScreen.js:513,530,532` — inside `if (isWeb) {` at :510 (visibilitychange handling). Reachable (MainStackNavigator → Warmup).
- `src/screens/CourseDetailScreen.js:881,882,990,992,1101,1103` — known state (do-not-re-litigate per brief), reconfirmed: `881-882` inside `if (isWeb) {` at :880, `990,992` inside `if (mode === 'web') {` at :989, `1101,1103` inside `if (isWeb) {` at :1097. Reachable (MainStackNavigator + ProfileStackNavigator → CourseDetail).
- `src/screens/PRsScreen.js:48,56,131,151` — all inside `typeof window !== 'undefined'` checks (fallback-navigation guards at :42, :55, :130, :150). Line 41 is a comment (false positive, see below). Reachable (ProfileStackNavigator → ExercisePanel).
- `src/screens/WorkoutExecutionScreen.js:1165,1490,1513,1542,1543,1545,1546` — :1165 has its own inline `typeof document !== 'undefined'` guard; :1490-1546 all inside `if (!isWeb) return;` at :1483 (this is the exact reference pattern the brief cites as "matches the codebase style"). Reachable (MainStackNavigator → WorkoutExecution).
- `src/screens/UpcomingCallDetailScreen.js:39,44,47,49` — `copyToClipboard()`; :39-49 inside `if (typeof document !== 'undefined') {` at :38 (falls back from `navigator.clipboard` first). Reachable (MainStackNavigator → UpcomingCallDetail).
- `src/components/LegalDocumentsWebView.js:37,42,47` — inside `if (!visible || !isWeb) return;` useEffect guard at :33. Imported by `ProfileScreen.js` (reachable).
- `src/components/EpaycoWebView.js:40` — inside `if (!visible || !checkoutURL || !isWeb) return;` at :37. Imported by `CourseDetailScreen.js` (reachable).
- `src/components/ErrorBoundary.js:63,104` — :63 inside `if (typeof window !== 'undefined') {` at :60; :104 inside nested `if (typeof window !== 'undefined') {` at :102, itself inside a block only rendered `{typeof window !== 'undefined' && (...)}` at :98. Wraps every screen via `withErrorBoundary`.
- `src/services/nutritionFirestoreService.js:15,16` — `trackFirstMealOnce()`; guarded by `if (typeof window === 'undefined' || !window.localStorage) return;` at :14. Imported by `ProfileScreen.js` (reachable).
- `src/services/sessionService.js:504,505` — inside `if (typeof window !== 'undefined' && window.localStorage) {` at :503 (first-workout activation event). Imported by `WorkoutExecutionScreen.js` and `DailyWorkoutScreen.js` (both reachable).
- `src/services/videoCacheService.js:169,198` — `preloadVideo()`; guarded by `if (!isWeb || typeof document === 'undefined') return;` at :154. Imported by `WarmupScreen.js` and `WorkoutExecutionScreen.js` (both reachable).
- `src/services/analyticsService.js:53,64,73,74` — `detectEnv()`/`readOptOut()`/`writeOptOut()`; each guarded by a local `isWeb()` helper (`typeof window !== 'undefined' && typeof document !== 'undefined'`, :26-28) via `if (!isWeb()) return ...`. Imported transitively by ~15 reachable files (AuthContext, ErrorBoundary, WarmupScreen, WorkoutExecutionScreen, CourseDetailScreen, sessionService, authService, etc). Note: the module itself imports `posthog-js` at top level — not a window/document literal so out of this sweep's scope, but flagged in the report's Concerns as a boot-crash risk to watch for.
- `src/services/profilePictureService.js:31,105,114,141,143` — all inside `if (isWeb) { ... } else { ... }` branches in `compressImage()` (:26-92) and `pickImage()` (:96-167). Imported by `OnboardingScreen.js`, `ProfileScreen.js`, `CourseDetailScreen.js`, `UpcomingCallDetailScreen.js` (all reachable).

## UNREACHABLE-FROM-NATIVE-NAV (only reachable via web-only entry points)

- `src/contexts/VideoUploadContext.js:184` — only imported by `App.web.js`. Native `App.js` imports `VideoContext.js` (a separate, unrelated file — same "Video" prefix, different module) for its `VideoProvider`. Confirmed via read of native `App.js`.
- `src/utils/backgroundSync.js:125,126,127` — only imported by `App.web.js`. (Also internally guarded: `if (typeof window === 'undefined') return;` at :121.)
- `src/utils/videoExchangeCompressor.js:8,28` — only imported by `VideoUploadContext.js`, itself unreachable (above).
- `src/utils/offlineError.js:5` — only imported by `nutritionApiService.js`, which is only imported by `NutritionScreen.js`, which is only referenced by `WebAppNavigator.jsx` (web-only navigator, not part of the native tree). (Also internally guarded: `if (typeof window === 'undefined') return;` at :4.)
- `src/utils/basePath.js:19` — only imported by `App.web.js` (native `App.js` does not import it). (Also internally guarded: `typeof window === 'undefined'` branch at :12.)
- `src/utils/wakeDebug.js:439,442,746,770,873` — only imported by `App.web.js`. Module-scope `if (IS_ENABLED) { window.addEventListener(...) }` (:438-445, :872) would execute at import time if it were ever imported natively, but it never is.
- `src/screens/LabScreen.js:951,952,969` — known state (per brief): not present in any native navigator (`AppNavigator`/`MainTabNavigator`/`MainStackNavigator`/`ProfileStackNavigator`/`OnboardingNavigator`). Only referenced by `WebAppNavigator.jsx` and `openBodyEntryFlag.js` (a util, not the screen). Confirmed via `grep -rln LabScreen src/navigation`.
- `src/services/bodyProgressService.js:20` — only imported by `LabScreen.js`/`LabScreen.web.js`, unreachable (above).

## FALSE POSITIVE (comment/identifier text, not an actual window./document. call)

- `src/utils/basePath.js:2` — comment: "...the inside of WebAppNavigator inspect window.location.pathname...".
- `src/utils/courseAccess.js:20` — comment mentioning "grace window", matched on "window" substring, not `window.` API access.
- `src/screens/PRsScreen.js:41` — comment: "// Try to navigate using window.location as fallback" (the actual call is :48, already triaged above).
- `src/services/exerciseHistoryService.js:75,134` — JSDoc comments: "Get a single session history document." / "Update userNotes on an existing session history document." — matched on "document" substring, not `document.` API access.

---

## Step 4: Native bundle check

`npx expo export --platform ios --clear` — PASS. `iOS Bundled ... index.js (1808 modules)`, exported to `dist/`, no module-resolution or DOM errors. No `src` changes were needed for this pass (0 VIOLATIONs from the triage above), so this simply reconfirms Task 3's export with the sweep now attested for Task 5.

## Step 5: Prebuild and boot the simulator

`npx expo prebuild --clean --platform ios` — first run failed at `pod install`: CocoaPods error "The following Swift pods cannot yet be integrated as static libraries: The Swift pod `AppCheckCore` depends upon `GoogleUtilities` and `RecaptchaInterop`, which do not define modules." This is the config-plugin issue the brief anticipated for a first SDK-54 prebuild. Fixed in `app.config.js` (not `ios/`, which is disposable) by installing `expo-build-properties` and adding:
```js
[
  "expo-build-properties",
  { ios: { useFrameworks: "static" } }
]
```
to the `plugins` array. Re-ran prebuild: `pod install` now succeeds (`✔ Installed CocoaPods`).

`npx expo run:ios` — **BLOCKED**: this machine has no Xcode.app installed, only Command Line Tools (`xcode-select -p` → `/Library/Developer/CommandLineTools`). Confirmed via `ls /Applications` (no Xcode*.app present) and `xcrun --find xcodebuild` / `xcrun simctl list` both failing with "unable to find utility ... not a developer tool or in PATH". `expo run:ios` itself detects this and halts non-interactively:
```
CommandError: Input is required, but 'npx expo' is in non-interactive mode.
Required input:
> Xcode must be fully installed before you can continue. If this message is still occurring after
  installing Xcode, you may need to finish the installation of the developer tools by running:
  `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`. Continue to the App Store?
```
No simulator could be listed, booted, or built to. Steps 5 (boot) and 6 (sign-in smoke) could not be executed — this is an environment gap (Xcode not installed), not a code defect. See task-5-report.md for full BLOCKED writeup.

## Step 6: Sign-in smoke by hand

Not reached — blocked by Step 5 (no simulator available to sign in on).
